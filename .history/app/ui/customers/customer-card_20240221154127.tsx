export default function CustomerCard({customers}):({customers: Array}){
    return(
        <div>
            {
                customers && customers.map((customer) => {

                })
            }
        </div>
    );
}