export default function CustomerCard({customers}):({customers: }){
    return(
        <div>
            {
                customers && customers.map((customer) => {

                })
            }
        </div>
    );
}