export default function CustomerCard({customers}):({customers: ReactNode}){
    return(
        <div>
            {
                customers && customers.map((customer) => {

                })
            }
        </div>
    );
}