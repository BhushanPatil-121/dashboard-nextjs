export default function CustomerCard({customers}):({customers: a}){
    return(
        <div>
            {
                customers && customers.map((customer) => {

                })
            }
        </div>
    );
}