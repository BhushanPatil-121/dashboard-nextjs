export default function CustomerCard({customers}){
    // console.log(customers)
    return(
        <div>
            {
                customers?.map(customer => (
                    <h1></h1>>
                ))
            }
        </div>
    );
}